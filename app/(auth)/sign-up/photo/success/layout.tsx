import { ReactNode } from "react";
import "@/styles/pages/sign-up-success.css";

export default function SignupPhotoSuccessLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
}
